x_choc = float(input())
a_rub = float(input())
y_toff = float(input())
b_rub = float(input())

price_choc = a_rub / x_choc
price_toff = b_rub / y_toff
ratio = price_choc / price_toff

print(price_choc)
print(price_toff)
print(ratio)
